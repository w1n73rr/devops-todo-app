apiVersion: 1
providers:
  - name: "cAdvisor"
    type: file
    options:
      path: /etc/grafana/provisioning/dashboards/dashboards/cadvisor.json

  - name: "Spring Boot Statistics"
    type: file
    options:
      path: /etc/grafana/provisioning/dashboards/dashboards/spring-boot.json

  - name: "k6"
    type: file
    options:
      path: /etc/grafana/provisioning/dashboards/dashboards/k6-results.json

  - name: "Node Exporter"
    type: file
    options:
      path: /etc/grafana/provisioning/dashboards/dashboards/node-exporter.json
